# Domain Taxonomy Package

Healthcare, Life Sciences, and technical track taxonomies are seeded via `scripts/seed.py` in Phase 1. This package will export shared JSON/YAML taxonomy assets for authoring tools.
